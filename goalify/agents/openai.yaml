interface:
  display_name: "Goalify"
  short_description: "Convert rough intent into a compact protected Codex /goal payload."
  default_prompt: "Use $goalify to turn my intention into a compact Codex /goal payload. Verify codex-goal first."
policy:
  allow_implicit_invocation: false
