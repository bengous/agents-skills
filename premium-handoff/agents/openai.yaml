interface:
  display_name: "Premium Handoff"
  short_description: "Brief selected targets for premium models."
  default_prompt: "$premium-handoff Prepare a compact premium-model handoff for this selected target."

policy:
  allow_implicit_invocation: false
