[package]
name = "wire-core"
version = "1.2.1"
edition.workspace = true
rust-version.workspace = true
include = ["Cargo.toml", "src/**"]

[features]
default = []
schema = []

[dependencies]
schema-data = { path = "../schema-data", version = "0.1.0", optional = true }
