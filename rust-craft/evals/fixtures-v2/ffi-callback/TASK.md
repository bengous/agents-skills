# Callback boundary repair

Finish the safe callback API for the parser binding. Preserve the foreign
contract and existing public signatures. Do not add dependencies.

Run the project checks and record changed files, safety decisions, commands,
results, and remaining gaps in `EVAL_REPORT.md`.
