# MSRV and feature fixture

This standalone crate advertises support for Rust 1.88. Its optional `json`
capability is backed by the local `json-stub` package, so validation requires
no registry access.

Run `./check.sh` to exercise the advertised configurations.
